# ai-commands

This folder is the canonical, platform-agnostic source for the advpl-specialist commands, used to generate prompt files for AI assistants other than Claude Code. Edit the files under `src/` and then run `node scripts/build-ai-commands.mjs` to regenerate the outputs. Outputs land in `.github/prompts/`, `.cursor/commands/`, and `.gemini/commands/`.
